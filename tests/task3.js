const { expect } = require('chai').use(require('chai-as-promised'));

var array = ['fizz', 'buzz', 'boom', 'buzz']

describe('Task3 - arrays', () => {

  it('turn array into one string then assert number of characters', () => {
    expect(array).to.equal()
  });

  it('remove duplicate values from array and assert array length is correct', () => {
    expect(array).to.equal(3)
  });

});
